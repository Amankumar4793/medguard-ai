# Frontend Environment Configuration Example
# Copy this file to .env or .env.production for custom backend endpoints

# Base URL for REST API (leave empty for same-origin proxy via Vite or Nginx)
VITE_API_URL=

# Socket.IO Server URL (leave empty for same-origin connection)
VITE_SOCKET_URL=
